import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Removed React.StrictMode for react-beautiful-dnd

ReactDOM.createRoot(document.getElementById('root')!).render(
    <App />,

)
